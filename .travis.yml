sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t aleksiuu/dockerized-react.ci -f Dockerfile.dev .

script:
  - docker run -e CI=true aleksiuu/dockerized-react-ci npm run test
